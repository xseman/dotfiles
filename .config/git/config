# defaults
[user]
	name = Filip Seman
	email = filip.seman@pm.me
	# signingKey =  ~/.ssh/id_rsa.pub

[push]
	default = current
	rebase = preserve

[pull]
	rebase = false

[init]
	defaultBranch = master

[core]
    # editor = code --wait --new-window
    editor = vim
    autocrlf = input
	quotepath = false

[column]
	ui = auto

[branch]
	sort = -committerdate

[commit]
	# gpgSign = true

[tag]
    # gpgsign = true

[gpg]
	format = ssh

# useful for multiple company/projects
# [includeIf "gitdir:~/work/<company>/"]
# 	path = .custom-config

[url "git@gitlab.com:"]
	insteadOf = https://gitlab.com/

[url "git@github.com:"]
	insteadOf = https://github.com/

[alias]
	undo = reset --soft HEAD^
